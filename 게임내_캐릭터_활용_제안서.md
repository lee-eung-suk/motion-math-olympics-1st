# 🎮 종목별 게임 화면 안에서 캐릭터 활용 제안서

썸네일은 원본 1~6.png로 되돌렸습니다. 그동안 종목별 유니폼 이미지(`char_u1_skate.png` ~ `char_u6_hockey.png` 6장)는 썸네일 자리를 비우면서 **아직 아무 데도 안 쓰이는 상태**입니다. 이 6장 + 이미 적용된 나머지(응원/승리/조준/골키퍼/장갑)를 각 종목의 **실제 플레이 화면**에 넣을 방법을 정리했습니다. 코드는 아직 안 건드렸습니다 — 방향 확인 후 진행하겠습니다.

---

## 먼저, 왜 종목마다 접근이 다른가

종목 1·5(스케이트·스키점프)는 **1인칭 시점**이 핵심 재미 요소입니다(제가 그린 발/스키만 보이고 얼굴이 안 보이는 이유). 여기에 캐릭터 전신을 플레이 화면 정중앙에 그대로 넣으면 "내가 달린다"는 몰입이 깨집니다. 그래서 이 두 종목은 **라이브 플레이 캔버스 안이 아니라, 그 앞뒤(대기·연출 컷)**에 배치하는 쪽으로 제안합니다.

종목 2·4·6(컬링·바이애슬론·하키)은 원래도 3인칭 시점이라 화면 안에 캐릭터를 세워도 자연스럽습니다.

종목 3(피겨)은 이미 제 손그림 스틱피겨가 그 자리에 있어서, **이미지로 교체**하는 게 제일 직접적입니다.

---

## 종목별 제안

### 종목1 — 목을 좌우로! 스케이트 (1인칭)
- **카운트다운 직후 트랜지션 컷**: "3·2·1" 끝나고 플레이가 시작되기 직전 0.3초 정도, `char_u1_skate.png`가 화면 중앙에 확 커지며 등장했다가 사라지고 1인칭 발로 전환 — "내가 이 캐릭터가 되어 달린다"는 연결고리를 줌
- (대안) HUD 우측 상단에 작은 원형 아바타로 항상 떠 있기 — 덜 극적이지만 구현은 더 간단

### 종목2 — 스톤을 굴려 10만들기 컬링
- **인트로 화면 강화**: 지금 `drawIntro()`가 "6 + ? = 10" 텍스트만 2.6초간 보여주는데, 여기에 `char_u2_curling.png`(스톤 미는 포즈)를 같이 배치 — 정적 화면이라 라이브 캔버스를 방해하지 않음
- (대안) 스톤을 놓는 홈 자리(`stoneHomePos()`) 뒤쪽에 작게 서 있는 모습으로 상시 배치

### 종목3 — 피겨 여왕의 모양 연기
- **직접 교체** ⭐ 가장 간단하고 효과적: 지금 `drawFigureSkater()`가 그리는 제 손그림 스틱피겨를 `char_u3_figure.png`로 교체
- 주의할 점: 지금 스핀 연출은 스틱피겨를 가로로 눌러 회전을 흉내내는 트릭인데(`ctx.scale`), 이미지에 그대로 쓰면 좌우로 눌리는 게 어색해 보일 수 있어 **단순 확대/축소 팝 효과**로 바꾸는 게 나을 것 같습니다

### 종목4 — 타겟 탕탕! 덧뺄셈 바이애슬론
- 커서는 이미 `char_u4_aim_icon.png`를 쓰고 있으니, **화면 하단에 전신 캐릭터**(`char_u4_biathlon.png`, 소총 든 포즈)를 서 있는 모습으로 추가 — 조준 커서와 별개로 "내 캐릭터가 여기서 쏘고 있다"는 배경 요소

### 종목5 — 규칙 타고 멀리 스키점프 (1인칭)
- **도약 대기 화면**: "🚀 지금 도약!" 게이지 옆에 `char_u5_skijump.png`를 작게 배치 — 이 구간은 카드가 아니라 순간 판정 화면이라 캐릭터를 넣어도 1인칭 몰입에 지장 없음
- (대안) 착지 성공 팝업("완벽한 착지! 12m!") 옆에 잠깐 등장

### 종목6 — 슛~골인! 아이스하키
- 골키퍼는 이미 장식으로 들어가 있으니, **퍽 시작 위치(`puckBase()`) 뒤쪽에 슈터로 배치**: `char_u6_hockey.png`(스틱 든 포즈)를 화면 하단에 세워두면 "내가 슛한다"는 느낌이 완성됨

---

## 정리표

| 종목 | 이미지 | 넣는 위치 | 난이도 |
|---|---|---|---|
| 1 스케이트 | char_u1_skate.png | 카운트다운→플레이 전환 컷 | 중 |
| 2 컬링 | char_u2_curling.png | 인트로 화면(정적) | 하 |
| 3 피겨 | char_u3_figure.png | 하단 실루엣 직접 교체 | 하 (스핀 연출만 조정) |
| 4 바이애슬론 | char_u4_biathlon.png | 화면 하단 상시 배치 | 하 |
| 5 스키점프 | char_u5_skijump.png | 도약 대기 화면 | 하 |
| 6 하키 | char_u6_hockey.png | 퍽 시작 위치 뒤 슈터 | 하 |

종목 2·3·4·5·6은 전부 "정적이거나 이미 있는 자리에 얹는" 수준이라 위험이 낮고, 종목1만 트랜지션 연출(타이밍 맞춘 등장/소멸)이라 살짝 손이 더 갑니다.

---

## ✅ 확인 부탁드립니다

1. 6개 다 한 번에 진행할까요, 아니면 쉬운 것(2·3·4·5·6) 먼저 하고 종목1은 나중에 볼까요?
2. 종목3의 스핀 연출을 "확대/축소 팝"으로 바꾸는 것, 괜찮으신가요?
